{
  "name": "js-renderer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "node scripts/deploy-gh-pages.mjs"
  },
  "prettier": {
    "printWidth": 100
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.8",
    "gh-pages": "^6.3.0"
  }
}
